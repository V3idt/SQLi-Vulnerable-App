<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Edit User</title>
    </head>
    <body>
        <% if ( user && user.is_admin) { %>
            <h1>Edit User</h1>
            <form action="/admin/edit/<%= user_to_edit.id %>" method="POST">
                <label for="username">Username:</label>
                <input type="text" placeholder="<%= user_to_edit.username %>" id="username" name="username" value="<%= user_to_edit.username %>" required>
                <br>
                <label for="first_name">First Name:</label>
                <input type="text" placeholder="<%= user_to_edit.first_name %>" id="first_name" name="first_name" value="<%= user_to_edit.first_name %>" required>
                <br>
                <label for="last_name">Last Name:</label>
                <input type="text" placeholder="<%= user_to_edit.last_name %>" id="last_name" name="last_name" value="<%= user_to_edit.last_name %>" required>
                <br>
                <label for="email">Email:</label>
                <input type="email" placeholder="<%= user_to_edit.email %>" id="email" name="email" value="<%= user_to_edit.email %>" required>
                <br>
                <label for="phone_number">Phone Number:</label>
                <input type="text" placeholder="<%= user_to_edit.phone_number %>" id="phone_number" name="phone_number" value="<%= user_to_edit.phone_number %>" required>
                <br>
                <label for="password">Password:</label>
                <input type="password" placeholder="<%= user_to_edit.password_hash %>" id="password" name="password" required>
                <br>
                <label for="is_admin">Is Admin:</label>
                <input type="checkbox" id="is_admin" name="is_admin" <%= user_to_edit.is_admin ? 'checked' : '' %> >
                <br>
                <input type="submit" value="Save Changes">
            </form>
        <% } else { %>
            <h1>Access Denied</h1>
            <p>You do not have permission to edit this user.</p>
            <a href="/">Back to Home</a>
        <% } %>
    </body>
</html>